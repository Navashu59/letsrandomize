#!/usr/bin/env node
"use strict";

const fs = require("fs");
const path = require("path");

const root = path.resolve(process.argv[2] || "public");

const replacements = new Map([
  [
    "All simulation and analysis happens locally in your browser. No data is sent to any server.",
    "Simulation inputs and results are processed in your browser. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "This tool runs entirely in your browser. No names you generate are sent to our servers or stored online. Your creativity remains private.",
    "Generated names and recent history are processed in your browser. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "Passwords generated by our tools are created entirely in your browser and are never transmitted to our servers.",
    "The password tool generates strings in your browser and does not intentionally submit those strings to our application servers."
  ],
  [
    "This tool runs entirely in your browser. Your list and the selection results are never sent to our servers. All processing happens locally on your device for maximum privacy and speed.",
    "List entries and selection results are processed in your browser. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "This tool works entirely in your browser—no data is sent to any server. Your generated emojis are stored locally and can be cleared anytime.",
    "Emoji selection and recent history are processed in your browser. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "<strong>Client-Side Only</strong>: No data is sent to servers - complete privacy protection",
    "<strong>Browser-based selection</strong>: Generated results are handled in your browser; service providers may still receive technical request data"
  ],
  [
    "No. All coin flips happen entirely in your browser. No data is stored on any server. History is saved locally in your browser.",
    "Coin results and recent history are processed in your browser. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "No. All coin flips happen entirely in your browser using JavaScript. No data is stored on any server. History is saved locally in your browser and you can clear it anytime.",
    "Coin results and recent history are processed in your browser, and you can clear local history at any time. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "No. All name generation happens in your browser. No data is sent to any server. Your history is stored locally in your browser and you can clear it anytime.",
    "Generated names and recent history are processed in your browser, and you can clear local history at any time. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "No. All name generation happens entirely in your browser using JavaScript. No data is ever sent to any server. The history feature uses your browser's localStorage, and you can clear it anytime.",
    "Generated names and recent history are processed in your browser, and you can clear the browser's local history at any time. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "No. All country selection happens entirely in your browser. The entire country database is embedded in the page. No server requests are made.",
    "Country selections use the database embedded in the page. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "No. All country selection happens entirely in your browser. The entire country database is embedded in the page and no server requests are made.",
    "Country selections use the database embedded in the page. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "No. All country selection happens in your browser. The entire country database is embedded in the page. No server requests are made.",
    "Country selections use the database embedded in the page. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "This tool works entirely in your browser—no data is sent to any server. Your generated words are stored locally and can be cleared anytime.",
    "Word selection and recent history are processed in your browser. Hosting, analytics, advertising, and security providers may still receive technical request data."
  ],
  [
    "<strong>Client-Side Processing</strong>: All word generation happens in your browser - no data is sent to servers",
    "<strong>Browser-based generation</strong>: Generated results are handled in your browser; service providers may still receive technical request data"
  ],
  [
    "Passwords are generated entirely in your browser and never sent to any server.",
    "The tool generates password strings in your browser and does not intentionally submit those strings to our application servers."
  ],
  [
    "No. Passwords are generated in your browser and are never transmitted or stored on any server. Only the local history feature stores recent results in your browser's localStorage.",
    "The tool generates password strings in your browser and does not intentionally submit them to our application servers. The optional history feature stores recent results in your browser's localStorage until you clear them."
  ],
  [
    "Your data stays in your browser and is never sent to a server.",
    "Wheel options and results are processed in your browser. If you share a URL that includes options, those options become part of the shared address; service providers may still receive technical request data."
  ]
]);

function walk(dir) {
  return fs.readdirSync(dir, { withFileTypes: true }).flatMap((entry) => {
    const target = path.join(dir, entry.name);
    return entry.isDirectory() ? walk(target) : [target];
  });
}

let changedFiles = 0;
let changedClaims = 0;
for (const file of walk(root).filter((target) => target.endsWith(".html"))) {
  const original = fs.readFileSync(file, "utf8");
  let html = original;
  for (const [from, to] of replacements) {
    if (html.includes(from)) {
      html = html.replaceAll(from, to);
      changedClaims += 1;
    }
  }
  if (html !== original) {
    fs.writeFileSync(file, html);
    changedFiles += 1;
  }
}

console.log(`Normalized ${changedClaims} privacy claim(s) in ${changedFiles} HTML file(s).`);
